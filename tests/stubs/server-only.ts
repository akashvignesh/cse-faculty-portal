// Vitest stub for the "server-only" guard package. The real module throws
// outside a React Server Components build; tests import server modules
// directly (API routes, query modules), so the guard is neutralized here.
export {};
